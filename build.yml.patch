--- a/.github/workflows/build.yml
+++ b/.github/workflows/build.yml
@@ -3,19 +3,9 @@
 on:
   workflow_dispatch:
   push:
-    branches:
-      - main
-    paths:
-      - ".github/workflows/build.yml"
-      - "docker/**"
-      - "tests/**"
-      - "*.sh"
+    branches: [master]
   pull_request:
-    paths:
-      - ".github/workflows/build.yml"
-      - "docker/**"
-      - "tests/**"
-      - "*.sh"
+    branches: [master]
 
 concurrency:
   group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
@@ -84,6 +74,17 @@
         with:
           fetch-depth: 50
           persist-credentials: false
+          submodules: true
+
+      - name: Patch
+        run: |
+          ./patch.sh
+          echo "COMMIT_SHA=$(git rev-parse HEAD:manylinux)" >> $GITHUB_ENV
+
+      - name: Start Docker
+        run: |
+          while ! sudo systemctl start docker; do sudo systemctl status docker || true; sleep 1; done
+          while ! docker info; do sleep 1; done
 
       - name: Set up QEMU
         if: matrix.platform == 'loongarch64' || matrix.platform == 'ppc64le' || matrix.platform == 'riscv64' || matrix.platform == 's390x'
@@ -101,14 +102,18 @@
           password: ${{ secrets.GITHUB_TOKEN }}
 
       - name: Build
-        run: ./build.sh
+        run: |
+          cd manylinux
+          ./build.sh
 
       - name: Deploy
-        if: needs.build_matrix.outputs.environment == 'quay.io'
-        run: ./deploy.sh
+        if: needs.build_matrix.outputs.environment == 'ghcr.io'
+        run: |
+          cd manylinux
+          ./deploy.sh
+
         env:
-          QUAY_USERNAME: ${{ secrets.QUAY_USERNAME }}
-          QUAY_PASSWORD: ${{ secrets.QUAY_PASSWORD }}
+          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
 
   all_passed:
     if: always()
